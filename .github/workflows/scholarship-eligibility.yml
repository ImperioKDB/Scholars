name: Scholarship eligibility review

on:
  workflow_dispatch: {}
  schedule:
    - cron: '45 */12 * * *'

permissions:
  contents: read

concurrency:
  group: scholarship-eligibility
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Load quality-ready candidates
        id: candidates
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          APP_URL: ${{ vars.APP_URL || 'https://www.scholars.com.ng' }}
        run: |
          set -euo pipefail
          test -n "$CRON_SECRET"
          response=$(curl --fail-with-body --silent --show-error --location --max-time 30 -H "Authorization: Bearer $CRON_SECRET" "$APP_URL/api/integrations/github/scholarship-eligibility")
          ids=$(echo "$response" | jq -c '.candidate_ids')
          echo "candidate_ids=$ids" >> "$GITHUB_OUTPUT"
      - name: Run advisory eligibility review
        if: steps.candidates.outputs.candidate_ids != '[]'
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          APP_URL: ${{ vars.APP_URL || 'https://www.scholars.com.ng' }}
          CANDIDATE_IDS: ${{ steps.candidates.outputs.candidate_ids }}
        run: |
          set -euo pipefail
          payload=$(jq -cn --argjson ids "$CANDIDATE_IDS" '{candidate_ids: $ids}')
          curl --fail-with-body --silent --show-error --max-time 300 \
            -X POST -H "Authorization: Bearer $CRON_SECRET" -H 'Content-Type: application/json' \
            --data "$payload" "$APP_URL/api/integrations/github/scholarship-eligibility"
